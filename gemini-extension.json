{
  "name": "semantic-grep",
  "version": "1.0.0",
  "description": "Codebase Indexer and Searcher with Persistent Memory",
  "mcpServers": {
    "agentContext": {
      "command": "${extensionPath}${/}dist${/}server",
      "args": [],
      "cwd": "${extensionPath}"
    }
  }
}
